public class EffectEnum
{
    public enum statusEffect { none, cc, dot, buff, debuff, terrainGen }

    public enum crowdControl { none, slow, stun, silence, grab, knockUp, knockBack }
    public enum dot { none, damage, healing }
    public enum buff { none, speedMultiplier, attackMultiplier, healMultiplier }
    public enum debuff { none, speedMultiplier, attackMultiplier, healMultiplier }
    //public enum terrainGen { none,  }
}
